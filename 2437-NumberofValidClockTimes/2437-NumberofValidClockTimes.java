                ans *= 4;
                ans *= 10;
            }
            else if(str[0] == '2') {
            ans *= 6;
        }
        if(str[1] == '?') {
            if(str[0] >= '0' && str[0] <= '1') {
        char[] str = time.toCharArray();
        int h0 = str[0] - '0', h1 = str[1] - '0';
        if(str[4] == '?') {
            ans *= 10;
        }
        if(str[3] == '?') {
class Solution {
    public int countTime(String time) {
        int ans = 1;
"
